version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:14.0.0
    steps:
      - checkout
      - run: npm install
      - run: npm run test
      - run: node app.js

workflows:
  build_and_test:
    jobs:
      - build 
